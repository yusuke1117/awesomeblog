<%# showing if the current_user and the other users have posts %>
<% if @feed_items.any? %>
    <ol class="microposts-lists">
        <%= render partial: 'shared/feed_item', collection: @feed_items %>
    </ol>
    <%= will_paginate @feed_items %>

<% end %>